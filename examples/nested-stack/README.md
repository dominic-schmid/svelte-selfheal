# Nested routes — `healer.stack()`

Multiple dynamic segments with optional static glue between them. Export layers from `$lib/healer.ts`, call `healer.stack()` in the load file.

Route: `/[id]/details/[innerId]`
